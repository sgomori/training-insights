# Async adapter only works within the same process, so for manually triggering cable updates from a console,
# and seeing results in the browser, you must do so from the web console (running inside the dev process),
# not a terminal started via bin/rails console! Add "console" to any action or any ERB template view
# to make the web console appear.
development:
  adapter: async

test:
  adapter: test

production:
  adapter: solid_cable
  connects_to:
    database:
      writing: cable
  # Every subscriber polls the same database the site is served from, and the
  # message it is waiting for is 15 to 30 seconds away. Ten polls a second buys
  # nothing a visitor can perceive and costs it against every other query on a
  # single small instance.
  polling_interval: 0.5.seconds
  message_retention: 1.day
